{
  "name": "spring-modernization-marketplace",
  "version": "1.12.0",
  "description": "Claude Code plugin marketplace for Spring ecosystem modernization. v1.12.0 makes all 52 skills portable to OpenAI Codex via standard YAML frontmatter, adds a validate:skills CI gate, and refreshes the markdown lint toolchain and GitHub Actions.",
  "private": true,
  "packageManager": "pnpm@11.0.0",
  "engines": {
    "node": ">=26",
    "pnpm": ">=11"
  },
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "validate:skills": "node scripts/validate-skills.mjs",
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\"",
    "lint": "markdownlint \"**/*.md\" --ignore node_modules",
    "lint:fix": "markdownlint \"**/*.md\" --ignore node_modules --fix",
    "link-check": "lychee --config lychee.toml --no-progress './**/*.md'",
    "link-check:offline": "lychee --config lychee.toml --no-progress --offline './**/*.md'",
    "check": "pnpm run validate:skills && pnpm run format:check && pnpm run lint",
    "check:all": "pnpm run check && pnpm run link-check",
    "fix": "pnpm run format && pnpm run lint:fix",
    "audit": "pnpm audit",
    "upgrade": "pnpm update --latest"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.49.1",
    "prettier": "^3.9.6"
  },
  "keywords": [
    "claude-code",
    "plugin",
    "marketplace",
    "spring-boot",
    "migration"
  ],
  "license": "MIT"
}
