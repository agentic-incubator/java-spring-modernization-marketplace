{
  "name": "spring-modernization-marketplace",
  "version": "1.5.0",
  "description": "Claude Code plugin marketplace for Spring ecosystem modernization with comprehensive breaking changes detection, OpenFeign migration, OpenAPI Generator intelligence, Spring Framework 7 support, and advanced validation capabilities",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\"",
    "lint": "markdownlint \"**/*.md\" --ignore node_modules",
    "lint:fix": "markdownlint \"**/*.md\" --ignore node_modules --fix",
    "check": "pnpm run format:check && pnpm run lint",
    "fix": "pnpm run format && pnpm run lint:fix",
    "audit": "pnpm audit",
    "upgrade": "pnpm update --latest"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.47.0",
    "prettier": "^3.7.4"
  },
  "keywords": [
    "claude-code",
    "plugin",
    "marketplace",
    "spring-boot",
    "migration"
  ],
  "license": "MIT"
}
